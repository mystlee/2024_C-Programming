#include <stdio.h>

int main(void) {
  int arr[10] = {0};
  int i;
  for (i = 0; i < 10; i++) {
    printf("%d\n", arr[i]);
  }
  // 45개의 숫자중에서 6개의 숫자를 랜덤하게 선택하고
  // 선택된 숫자가 중복되지 않은지 체크한 이우에
  // 선택된 배열 추력하는 프로그램 작성하려면?
  
  return 0;
}